# Committer Command

Generate a clean conventional commit for the completed and verified changes:
1. Review `git status` and `git diff` for the task.
2. Stage only the relevant files (no temp files, caches, or unrelated modifications).
3. Execute `git commit` with a concise conventional commit subject (e.g. `feat(...)`, `fix(...)`, `docs(...)`).
